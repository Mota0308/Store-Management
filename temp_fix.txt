  // 獲取排序箭頭圖標
  const getSortIcon = (locationId: string) => {
    const state = locationSortStates[locationId] || 'default'
    switch (state) {
      case 'desc':
        return '' // 向上箭頭
      case 'asc':
        return '' // 向下箭頭
      default:
        return '' // 雙向箭頭
    }
  }
